{
  "name": "app",
  "version": "1.0.0",
  "description": "CSS related learning",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch scss:css landing-page/src:/landing-page/src --poll",
    "browser-sync": "browser-sync start --server --files 'css/*.css, landing-page/src/*.css, index.html, landing-page/src/*.html'",
    "dev": "npm-run-all --parallel sass browser-sync"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
