{
  "name": "app",
  "version": "1.0.0",
  "description": "CSS related learning",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch scss:css landing-page/src/style-landing-page.scss:landing-page/src/style-landing-page.css --poll",
    "browser-sync": "browser-sync start --server --files 'css/*.css, index.html, landing-page/src/*.css, landing-page/src/*.html' --no-open",
    "dev": "npm-run-all --parallel sass browser-sync"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
